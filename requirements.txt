# ============================================================
# Smart Vertical Streaming — Python Dependencies
# ============================================================
#
# SETUP INSTRUCTIONS
# ------------------
# 1. Clone the repository:
#       git clone <repo-url>
#       cd Disney-SD-Smart-Vertical-Streaming
#
# 2. Create a virtual environment:
#       python -m venv venv
#
# 3. Activate it:
#       Windows:  venv\Scripts\activate
#       Mac/Linux: source venv/bin/activate
#
# 4. Install all dependencies (including ByteTrack from GitHub):
#       pip install -r requirements.txt
#
#    Note: If the ByteTrack git install fails, clone manually:
#       git clone https://github.com/ifzhang/ByteTrack.git
#       cd ByteTrack && pip install -e . && cd ..
#
# 5. YOLOv8 weights are downloaded automatically on first run
#    by the ultralytics package.
# ============================================================

# Core computer vision
ultralytics==8.4.30
opencv-python==4.13.0.92
numpy==2.2.6

# Tracking (ByteTrack — provides yolox.tracker.byte_tracker)
# Used by: cropping/reframe_9x16.py, tests/track_test.py
yolox @ git+https://github.com/ifzhang/ByteTrack.git

# UI
streamlit>=1.35.0

# Utilities
tqdm>=4.66.0
kagglehub>=0.3.0
