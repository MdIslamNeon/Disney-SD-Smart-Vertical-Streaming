# ============================================================
# Smart Vertical Streaming — Python Dependencies
# ============================================================
#
# SETUP INSTRUCTIONS
# ------------------
# 1. Clone the repository:
#       git clone <repo-url>
#       cd Disney-SD-Smart-Vertical-Streaming
#
# 2. Create a virtual environment:
#       python -m venv venv
#
# 3. Activate it:
#       Windows:  venv\Scripts\activate
#       Mac/Linux: source venv/bin/activate
#
# 4. Install all dependencies:
#       pip install -r requirements.txt
#
# 5. YOLOv8 weights are downloaded automatically on first run
#    by the ultralytics package.
# ============================================================

# Core computer vision
ultralytics==8.4.30
opencv-python==4.13.0.92
numpy==2.2.6
lap>=0.4.0  # Linear assignment solver required by ultralytics trackers (BOTSORT/ByteTrack)

# ByteTrack tracking is provided by ultralytics built-in tracker:
#   model.track(tracker="bytetrack.yaml")
# No separate yolox/ByteTrack package needed.

# UI
streamlit>=1.35.0

# Utilities
tqdm>=4.66.0
kagglehub>=0.3.0
scipy>=1.13.0
