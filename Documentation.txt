Disney SD Smart Vertical Streaming — Project Documentation
==========================================================

PROJECT STRUCTURE
-----------------
Disney-SD-Smart-Vertical-Streaming/
├── UI/
│   ├── StreamJup.py          # Main Streamlit app (YOLOv8 person detection UI)
│   └── Streamlit_UI.py       # Alternate/legacy Streamlit UI
├── cropping/
│   ├── read-and-crop.py      # Basic video read and crop
│   ├── reframe_9x16.py       # 9:16 auto-reframing with ByteTrack
│   ├── smartCroppingImages.py
│   └── smartCroppingVideos.py
├── models/
│   ├── yolov8n.pt            # YOLOv8 nano weights
│   ├── yolov8s.pt            # YOLOv8 small weights
│   └── yolov8m.pt            # YOLOv8 medium weights (used by UI)
├── tests/
│   ├── basketballTest.py
│   ├── read-and-crop-test.py
│   └── track_test.py
├── ByteTrack/                # ByteTrack submodule/clone
├── sd_disney/                # Python virtual environment
├── requirements.txt
├── run_ui.bat                # One-click script to launch the UI
└── Documentation.txt


SETUP
-----
1. Clone the repository:
      git clone <repo-url>
      cd Disney-SD-Smart-Vertical-Streaming

2. Create the virtual environment:
      python -m venv sd_disney

3. Activate it:
      Windows (PowerShell):  .\sd_disney\Scripts\Activate.ps1
      Windows (CMD):         sd_disney\Scripts\activate.bat
      Mac/Linux:             source sd_disney/bin/activate

4. Install dependencies:
      pip install -r requirements.txt

   Note: If the ByteTrack git install fails, clone manually:
      git clone https://github.com/ifzhang/ByteTrack.git
      cd ByteTrack && pip install -e . && cd ..


RUNNING THE UI
--------------
From the project root, double-click or run:
      .\run_ui.bat

This launches StreamJup.py via Streamlit without needing to cd into the UI folder.

If the .bat launchers are broken (stale venv path), bypass them with:
      & ".\sd_disney\Scripts\python.exe" -m streamlit run UI\StreamJup.py


KEY DEPENDENCIES
----------------
- ultralytics    — YOLOv8 model inference
- opencv-python  — Video reading and frame processing
- streamlit      — Web UI
- ByteTrack      — Multi-object tracking (via yolox package)
- numpy, tqdm, kagglehub

---------------------------------------------------
Summary

Smart Vertical Basketball Streaming ΓÇö Session Summary
======================================================

PROJECT OVERVIEW
----------------
Disney Senior Design Project: Smart Vertical Basketball Streaming
Primary goal: Automatically reframe landscape basketball video to 9:16 vertical format using YOLO detection + ByteTrack tracking.

FILES MODIFIED / CREATED
-------------------------
- UI/StreamJup.py         ΓåÆ Main Streamlit UI (active file)
- UI/Streamlit_UI.py      ΓåÆ Previous UI (functionality migrated to StreamJup.py)
- run_ui.bat              ΓåÆ Launches StreamJup.py via sd_disney virtual environment
- ball_framing_integration.py ΓåÆ Diagnosed but not modified (ball detection issue)

FEATURES IMPLEMENTED IN StreamJup.py
--------------------------------------
1. Video upload via Streamlit file uploader
2. YOLO person detection (yolov8m.pt, class 0, conf=0.50, iou=0.95)
3. Frame Scrubber tab ΓÇö slider to browse frames with detection overlay
4. Play Video tab:
   - Renders clean frames to H.264 (avc1) MP4 via OpenCV VideoWriter
   - Serves video over a local HTTP server (avoids Streamlit 200MB websocket limit)
   - HTML5 <video> + <canvas> overlay with requestAnimationFrame loop
   - Per-frame bounding box data embedded as JSON in the HTML
   - Toggle button to show/hide boxes during playback (no page reload)
5. Metrics: total frames, avg people/frame, peak people

BUGS FIXED DURING SESSION
--------------------------
1. Play Video button not visible        ΓåÆ Switched from radio buttons to st.tabs
2. Video not playing (black player)     ΓåÆ Switched from mp4v to avc1 (H.264) codec
3. FileNotFoundError for ffmpeg         ΓåÆ Removed subprocess; avc1 works natively on Windows 10
4. use_container_width deprecation      ΓåÆ Replaced with width='stretch' throughout
5. Websocket 200MB limit exceeded       ΓåÆ Replaced base64 embedding with local HTTP server
6. Video letterboxed / boxes misaligned ΓåÆ Changed CSS from object-fit:contain to width:100%; height:auto
7. Toggle button cut off at bottom      ΓåÆ Moved controls div above player-wrap
8. iframe too short (video cut off)     ΓåÆ Changed container width estimate from 900px to 1400px
9. OpenH264 terminal warnings           ΓåÆ Added OPENCV_LOG_LEVEL and OPENCV_FFMPEG_LOGLEVEL env vars

KNOWN / PENDING ISSUES
-----------------------
- Ball detection (ball_framing_integration.py): yolov8m.pt general COCO model
  misidentifies the ABC logo as a sports ball (class 32). Proposed fixes:
  a) Increase CONFIDENCE_THRESHOLD (currently 0.20)
  b) Add region exclusion for logo area
  c) Use a sports-specific fine-tuned YOLO model
  ΓåÆ No code changes made yet; awaiting implementation

- iframe height fix (1400px estimate) may still clip video bottom on very wide/tall displays.
  If still cut off, increase estimate to 1600px or implement dynamic height via JS postMessage.

TECH STACK
----------
- Python 3.x, OpenCV 4.13, YOLOv8 (ultralytics 8.4.30), NumPy 2.2.6
- Streamlit >= 1.35.0, ByteTrack (yolox), SciPy, tqdm
- HTML5 Canvas API, requestAnimationFrame, Python http.server (threading)
- Virtual env: sd_disney (Windows, located at repo root)
